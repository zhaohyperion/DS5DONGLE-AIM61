# Changelog

All notable changes to DS5DONGLE-AIM61 are documented here.

---

## Unreleased

### Added
- v3.6.0 新增 `0xFB` v3 19 控件一对多同步映射、`0xFE` v3 宏传输，以及宏 A/B 持久化、代数冲突保护和回读校验
- 新增时序、连发、重复、循环宏，支持按键、摇杆、扳机、双触摸点、随机时长、4 个档位和物理 PS 2 秒急停
- 新增无需工具的 Create+Options 独立录制，以及 Mute+Options 完全隔离的设备宏管理模式
- Windows 工具 v1.4.0 改为“测试中心 / 按键映射 / 设备调试 / 固件刷写”四页签，并新增宏时间轴和设备宏读取/写入
- Windows 原生刷写器新增测试中心、设备调试、自动压力测试、七页运行诊断和 M61 内部桥接延迟统计
- 设备调试页新增 250/500/实时轮询档位读取与“应用并重启”，仅修改轮询字段并保留其余设备配置
- 测试中心新增双摇杆实时轨迹、松手中心偏移、轴范围、48 方向覆盖率和 RMS 圆度误差分析
- 摇杆分析新增保留原值的“正常 / 建议复测 / 建议校准”三级经验结论，未完成采样时不判故障
- 新增“先完成松手中心与双摇杆轨迹分析、再显式确认”的 DualSense 永久中心/范围校准向导；M61 固件仅转发结构严格合法的 `0x82` 请求并刷新 `0x83` 状态缓存
- 将引导测试、摇杆分析、校准前后对比、音频确认、HID 性能、压力负载和 M61 快照整合为一份完整 JSON 测试报告，并明确标记未测试/部分完成项目
- 完整测试报告新增中文摘要，集中显示总体结论、异常项、未测试项、关键指标和处理建议
- 按 `dualsense-tester` / `dualshock-tools` 当前实现校正六轴 Y/Z 映射和 1 kHz 扬声器/耳机音量，并新增左右依次震动、RGB/玩家灯动画及三轮扳机自动复位引导信号
- DualSense Edge 永久摇杆校准改用上游定义的双阶段提交与状态确认；普通 DualSense 继续使用单阶段协议
- 麦克风录音新增 RMS、峰值、有效声音窗口、静音底噪、信噪比和双声道一致性分析，结果显示在测试中心并写入统一报告
- 对齐 `dualshock-tools` 的圆度提示：RMS 圆度误差低于 5% 时标记“建议复测”，但不归类为故障或建议永久校准
- 按上游状态字节规则显示 5% 中点电量档位、充电/外接电源状态以及 3.5 mm 耳机和耳麦麦克风连接状态
- 输入发布改为“模拟量约 60 Hz + 数字/触摸/插孔变化立即上报”，避免高轮询固件下极短按键被 UI 限频漏采
- 引导诊断新增全程输入覆盖追踪：面键、功能键、方向键八方向、肩键、双摇杆八方向/回中、L2/R2 全量程、双触摸点/四向滑动和六轴响应均自动判定；只有灯效、震动、扳机阻力、声音等主观输出保留人工确认
- 引导式设备诊断卡片新增独立可见的 JSON 导出入口；测试中途和完成后均可导出统一测试报告，部分完成、缺失输入和已采集 M61 快照会按原状态保留
- 固件刷写、测试中心和设备调试改为整页纵向滚动，移除会在小窗口中抢占或失去滚轮事件的嵌套滚动区域
- 测试中心的 L2/R2 自适应扳机分别新增 `0–255` 力度调节，并保留高强度短时使用提示；引导诊断和压力测试继续采用固定低强度
- 独立按键映射页新增 19 控件一对多编辑器，覆盖 15 个按键与方向键四向，支持禁用、同步组合、写入后回读校验和恢复默认；固件使用 `btn_remap_v3` 与 `0xFB` v3，可导入旧 19 键 v2 单目标表，但不读取旧 15 键映射
- 测试中心实时输入改为可缩放的高对比度 DS5 矢量图：直接采用并本地化 `daidr/dualsense-tester` 固定提交的 SVG 路径、`1117×892` 模型坐标、控件布局、摇杆归一化和双触摸点映射；按物理位置显示所有输入，活动输入统一以蓝色点亮，并继续保留精确轴值与传感器数据
- 麦克风测试改用 WASAPI 自动选择 M61/DualSense 输入端点，录音与默认输出回放分别记录，并可保存最近一次 WAV
- 固件刷写页新增本地签名 OTA ZIP 手动选择、电脑端固定公钥验签、执行前二次校验，以及重启后 USB 重新枚举/版本/配置/诊断快照确认
- 本地 UART ZIP 和解包目录强制验证 `SHA256SUMS.txt`，界面显示实际来源路径并在刷写失败时保留临时运行目录供排查

### Changed
- 设备测试、诊断和固件管理统一迁移到 Windows 原生工具
- 公开配置网站已下线，不再提供网页配置、网页诊断或网页 OTA
- High-Speed 新配置默认实时档（约 750 Hz）；既有持久化设置保持不变
- 音频输出重采样改为 Q15 热路径，缓存震动增益换算，并使用对齐打包优化麦克风单声道复制
- 手柄快速重连时保留 Windows HID/UAC 会话 10 秒；Primer 在连接稳定后带配置音量重发
- 主机持续休眠时关闭 BR/EDR page/inquiry scan，恢复后按最新 USB 状态重新启用
- 宏关闭或未配置时跳过高频转发路径中的 19 控件触发扫描，并避免设备启动后的第一次按键被误判为双击
- Standard/Diagnostic 使用相同 HID Feature 描述符布局并按固件版本生成 `bcdDevice`，避免 Windows 在同 VID/PID 下缓存错误的 `0xFD` 能力；Standard 仍不创建任何诊断任务或快照

### Removed
- 移除仓库中的独立配置网页、前端测试及托管配置

---

## v3.15 — 2026-08-06

### Added
- 新增自定义手柄连接后灯光颜色，可在配置页面选择

### Fixed
- 修复调整系统音量后手柄灯光熄灭的问题，修复绝区零角色 LED 灯光熄灭问题
- 修复第二次进入游戏后手柄 LED 灯光不亮的问题
- 修复 Steam 输入转换游戏中震动延迟和持续震动的问题
- 可能修复切换手柄时第二个手柄音频断断续续的问题
- 修复断开后重连时看门狗误触发导致立即断联
- 修复音频流关闭后 USB 端点状态残留，提升传输稳定性
- 修复电脑待机恢复后手柄可能无响应的问题

### Changed
- 降低空闲时功耗和发热
- 麦克风缓冲区扩容，减少爆音

---

## v3.14 — 2026-08-02

### Added
- 新增双版本固件：普通版（兼容性优先）和高轮询率版（最高 750Hz）

### Changed
- 默认固件切换为普通版（Full-Speed），USB 线材兼容性更好

### Improved
- 优化音频编码性能，降低 CPU 开销

---

## v3.13 — 2026-08-01

### Improved
- 优化重连成功率

---

## v3.12 — 2026-07-30

### Added
- 支持记忆多个手柄（最多 8 个），单击按钮快速切换
- 游戏中手柄断连重连后，自动恢复自适应扳机状态

### Improved
- 大幅优化蓝牙配对和重连效率，手柄从其他设备切换回来无需手动清除

### Changed
- 双击按钮改为搜索新手柄配对（原：软重启）
- USB 速度上限调整为 1000Hz

### Fixed
- 可能优化了扳机响应速度

---

## v3.11 — 2026-07-29

### Added
- 实时档位轮询率从 ~500Hz 提升至 ~750Hz

### Changed
- 连接成功后手柄灯光默认改为白色
- 默认开启 USB 隐身模式
- 默认开启 1 分钟后自动关闭指示灯

### Fixed
- 修复高负载下可能崩溃的问题
- 优化蓝牙重连响应速度，减少手柄低电量断开后重连失败的概率

---

## v3.10 — 2026-07-25

### Fixed
- 优化陀螺仪数据传输稳定性，减少瞄准时的抖动
- 更新 SDK 蓝牙控制器库至 v2.3.30-RC1

---

## v3.9 — 2026-07-24

### Added
- 按键映射（Button Remap）固件基础功能：旧协议支持 15 个数字按键（后续由 v3.5.2 的 19 控件 v2 协议取代；不做旧数据迁移）
- 新增 HID Feature Report 0xFB 用于主机工具读写映射表

### Fixed
- 修复首次连接失败率高：缩短各超时（CONNECTING 15s→8s，DISCONNECTING 5s→1.5s，L2CAP CFG 4s→2s）
- OTA 版本字符串修复（原误显示为 event_v1.1.1）
- 音量映射修正：Windows dB 范围正确映射到 DualSense [0,127]，解决音量偏小问题
- 键盘接口仅在 PS 快捷键启用时包含，避免额外 USB 接口干扰游戏自适应板机和音频
- 修复 USB 唤醒：无键盘接口时正确设置 REMOTE-WAKEUP 标志位

---

## v3.8 — 2026-07-21

### Fixed
- 修复 Linux（Bazzite / PipeWire）下扬声器播放卡顿的问题
- 修复播放音乐时不操作手柄会自动断开连接的问题

---

## v3.7 — 2026-07-20

- Earlier LED primer + stealth mode purple on reconnect
- Stealth primer hold-forward logic and primer logs

## v3.6 and earlier

See git log for details.
